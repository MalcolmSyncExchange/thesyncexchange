# RETIRED — historical SQL evidence

Do not execute these SQL files. They can restore superseded role/moderation guards, removed auth triggers, or broad Storage permissions. The files remain byte-for-byte intact for baseline regression tests and investigation.

Use [canonical deployment preflight](../../docs/security-pr2/deployment.md). The same warning applies to `../storage-policies.sql`.
